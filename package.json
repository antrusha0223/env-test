{
  "name": "env-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "serve": "^14.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "serve -s build",
    "dev": "REACT_APP_ENV=development react-app-rewired start",
    "build:dev": "REACT_APP_ENV=development react-app-rewired build",
    "build:staging": "REACT_APP_ENV=staging react-app-rewired build",
    "build:prod": "REACT_APP_ENV=production react-app-rewired build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "customize-cra": "^1.0.0",
    "react-app-rewired": "^2.2.1"
  }
}
